extends layout

block content
    .row.banner
        .col-12
            h1= set.title

    .row
        .col-12.col-md-8
            .quizsets
              // each question in set.questions
              //     .card
              //         .card-block
              //             p= question.qTitle
